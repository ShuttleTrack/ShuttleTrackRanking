/** Shared desktop header + row grid (no gap — must match on both). */
export const LEADERBOARD_DESKTOP_GRID =
  'hidden md:grid grid-cols-[4.5rem_minmax(10rem,1.6fr)_7.5rem_repeat(4,minmax(0,1fr))] items-center';

/** Site-root public aggregate board (no last day / trend). */
export const PUBLIC_LEADERBOARD_DESKTOP_GRID =
  'hidden md:grid grid-cols-[4.5rem_minmax(10rem,1.6fr)_7.5rem_repeat(2,minmax(0,1fr))] items-center';
